using System;

namespace VideoProjekt
class Program
{
    static void Main(string[] args)
    {
        // TODO: Erstelle Instanzen von Video, Comment, VideoManager etc.
            // Beispiel:
            // VideoManager videoManager = new VideoManager();
            // Video video = new Video("Titel", "Autor", 120);
            // Comment comment = new Comment("Kommentarautor", "Kommentartext");
            // video.AddComment(comment);
            // videoManager.AddVideo(video);
            // videoManager.DisplayAllVideos();
    }
}